import { ILogin } from './page';

const base: ILogin = {
  providersLoginText: 'continue with',
  providers: undefined,
  Login: undefined,
  locale: '',
};

export const mockBaseTemplateProps = {
  base,
};
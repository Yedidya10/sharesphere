import { ICurrentUserBorrowedItems } from './CurrentUserBorrowedItems'

const base: ICurrentUserBorrowedItems = {
  sampleTextProp: 'Hello world!',
  label: '',
}

export const mockCurrentUserBorrowedItemsProps = {
  base,
}

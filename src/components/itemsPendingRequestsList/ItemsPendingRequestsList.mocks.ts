import { IItemsPendingRequestsList } from './ItemsPendingRequestsList'

const base: IItemsPendingRequestsList = {
  sampleTextProp: 'Hello world!',
  label: '',
}

export const mockItemsPendingRequestsListProps = {
  base,
}

import { IMessagesDrawer } from './MessagesDrawer'

const base: IMessagesDrawer = {
  sampleTextProp: 'Hello world!',
  label: '',
  drawerWidth: 0,
}

export const mockMessagesDrawerProps = {
  base,
}
